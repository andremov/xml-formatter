type LocaleStrings = {
  error: string;
  error_body: string;
  title: string;
  labels: {
    data: string;
    input: string;
    output: string;
  };
  buttons: {
    copy: string;
    copied: string;
  };
};

export default LocaleStrings;
