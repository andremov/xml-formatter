type LocaleStrings = {
  error: string;
  error_body: string;
  title: string;
  owner: string;
  author: string;
  description: string;
  labels: {
    data: string;
    input: string;
    output: string;
  };
  buttons: {
    copy: string;
    copied: string;
    minimize: string;
    expand: string;
    sample: string;
  };
};

export default LocaleStrings;
